#include <stdio.h>

/**
* main - cause an infinite loop
* Return: 0
*/

int main(void)
{
	int i;

	printf("infinite loop incoming :(\n");

	i = 0;
	/**
	*while (1 < 10)
	*{
	*putchar(i);
	*}
	*/
	printf("infinite loop voided! \\o/\n");

	return (0);
}
